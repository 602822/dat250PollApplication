## Using Docker PostgreSQL image
It was conveinient to use. It was easy to implement, i just needed to download the docker image and add some dependencies. 
I had trouble creating the database tables from the sql script using DbBeaver so i used InteliJ's integrated Database Client instead

## Creating my own Docker Image
I created a DockerFile for my backend REST api. I followed the example from the teacher and an example i found online. It was not difficult and it worked smoothly
